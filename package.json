{
  "name": "voxelgame",
  "version": "0.1.0",
  "description": "Minecraft-style voxel engine and map editor with 0.5m and 1m voxels. Runs offline from a single index.html.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "build": "npm run build:editor && npm run build:game",
    "build:editor": "esbuild src/main.js --bundle --format=iife --outfile=build/game.js --sourcemap --banner:js='/* bundle generated by esbuild — do not edit by hand */'",
    "build:game": "esbuild src/game/gameMain.js --bundle --format=iife --outfile=build/game-play.js --sourcemap --banner:js='/* bundle generated by esbuild — do not edit by hand */'",
    "server": "node server.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "playwright-core": "^1.62.1",
    "pngjs": "^7.0.0"
  }
}
